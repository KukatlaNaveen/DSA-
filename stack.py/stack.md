1.what is stack explain lifo
stack is a linear data structure lifo last in first out means we append element to end and remove from end of the list or stack
2.name real world examples of stack usage
in real world when we  use in browser and open you tube and instagram it store in stack when we come back first close instagram tab next you tube and google and also used in recursion
3.how can you implement stack using a list in python
when we implement the stack by using list we have append and pop method to add elements and remove elements
4.what is the time complexity of push and pop
the time complexity of push O(1) amortized
                    pop is O(1)
how stack is used in function call management
when function call then that push onto the stack after finishes then pop from the stack