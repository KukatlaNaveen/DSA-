why are strings are immutable in python
stings are immutable because once you created we can not change again ex:s="hello" s[0]='t' this can give error
2.what is the time complexity of strings concatenaiton using + in a loop
string time complexity is O(n^2) becuse strings are immutable in each iteration create a new string and copy to it